{
  "name": "@pkgs/mcp-server",
  "version": "0.1.0",
  "main": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "test": "bun test",
    "check": "tsc --noEmit",
    "format": "prettier --write src"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "@pkgs/pty-manager": "workspace:*",
    "@pkgs/session-manager": "workspace:*",
    "consola": "^3.4.2",
    "fetch-to-node": "^2.1.0",
    "hono": "^4.9.10"
  }
}
